{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "disposition",
    "speech_acts",
    "explicit_requirements",
    "assumptions",
    "optimized_prompt"
  ],
  "properties": {
    "disposition": {
      "type": "string",
      "enum": [
        "rewrite",
        "pass_through"
      ]
    },
    "speech_acts": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "explicit_requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "source_quote",
          "normalized"
        ],
        "properties": {
          "source_quote": {
            "type": "string",
            "minLength": 1
          },
          "normalized": {
            "type": "string"
          }
        }
      }
    },
    "assumptions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "text",
          "confidence"
        ],
        "properties": {
          "text": {
            "type": "string"
          },
          "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          }
        }
      }
    },
    "optimized_prompt": {
      "type": "string"
    }
  }
}
